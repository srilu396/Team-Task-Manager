{
  "name": "tasknova",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\" \"node open-browser.js\"",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "seed": "cd backend && node seed/seed.js"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
